<resources>
    <!-- Base application theme. -->
    <style name="Theme.CleanArchitectureSample" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->

        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>


        <!-- 앱바 스타일 -->
        <item name="appBarLayoutStyle">@style/Theme.App.AppBarLayout</item>
        <!-- 툴바 스타일 -->
        <item name="toolbarStyle">@style/Theme.App.Toolbar</item>
        <!--        <item name="collapsingToolbarLayoutStyle">@style/Theme.App.CollapsingToolbarLayout</item>-->
    </style>

    <!-- AppBarLayout 스타일 -->
    <style name="Theme.App.AppBarLayout" parent="Widget.Design.AppBarLayout">
        <item name="android:background">@color/white</item>
    </style>

    <!-- Toolbar 스타일 -->
    <style name="Theme.App.Toolbar" parent="Widget.MaterialComponents.Toolbar">
        <item name="titleTextAppearance">@style/Appearance.App.Toolbar.Title</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="Appearance.App.Toolbar.Title" parent="">
        <item name="android:textSize">16sp</item>
    </style>

</resources>